{
  "name": "notebackend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -fr build && sc ../../Fullastack-2022/testi2/ && npm run build && cp -r build ../osa3",
    "deploy": "git push heroku main",
    "deploy-full":"npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "Topi Kanninen",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "heroku": "^7.60.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
